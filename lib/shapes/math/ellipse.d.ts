declare const _default: {
    xAt(psi: number, rx: number, ry: number, cx: number, t: number): number;
    yAt(psi: number, rx: number, ry: number, cy: number, t: number): number;
    xExtrema(psi: number, rx: number, ry: number): number;
    yExtrema(psi: number, rx: number, ry: number): number;
};
export = _default;
